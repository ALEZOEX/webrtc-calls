version: '3.8'

services:
  webrtc-app:
    build: .
    container_name: webrtc-calls
    ports:
      - "3030:3030"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: always
    networks:
      - webrtc-network

networks:
  webrtc-network:
    driver: bridge